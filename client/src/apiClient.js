/* Let's keep a single api client to make requests to the backend. */

export const createClient = () => {};

export const addProduct = () => {};

export const createPayment = () => {};

export const updatePayment = () => {};

export const capturePayment = () => {};